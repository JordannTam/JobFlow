import type { JSX } from "react";

/**
 * StatCard component props.
 *
 * Fields:
 *     label (string)   - Stat label (e.g., "Applied")
 *     value (number)   - Stat value (e.g., 12)
 *     color (string)   - Optional, Tailwind color class
 */
// interface StatCardProps {
//   label: string;
//   value: number;
//   color?: string;
// }

/**
 * Dashboard stat display card.
 * Shows a label and numeric value with optional color accent.
 *
 * Arguments:
 *     props (StatCardProps) - Component props
 *
 * Return Value:
 *     Returns JSX.Element
 */
// { label, value, color }: StatCardProps
export const StatCard = (): JSX.Element => {
  throw new Error('Not implemented');
};
